let firstname = "john"; // variable that can be reassigned
const age = 20; // contant variable that cannot be reassigned
var isStudent = true; // Older way to declare variables.

console.log(firstname);
console.log(age);
console.log(isStudent);
